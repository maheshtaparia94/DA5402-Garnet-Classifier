[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "garnet-classifier"
version = "0.1"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
